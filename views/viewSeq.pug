extends index.pug

block content
    ul
    each move in moves
        li.displayMoveinSeqCard 
            a(href=`/poses/${move.pose_id}` target="_blank")
            h2= move.meta_title
            img(src=`http://yoga.com/${move.cover_thumbnail}` alt="move.meta_title")